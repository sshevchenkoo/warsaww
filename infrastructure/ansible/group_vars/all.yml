# Secrets and config — values come from root .env via Makefile --extra-vars
---
project_name: transcendence

# PostgreSQL
postgres_private_ip: "10.0.1.20"
postgres_db:         "transcendence"
postgres_user:       "transcendence"

# Hetzner token — for hcloud dynamic inventory plugin
hcloud_token: "{{ lookup('env', 'HCLOUD_TOKEN') }}"

# k3s
k3s_version: "v1.29.4+k3s1"

# ELK
elk_private_ip: "10.0.1.30"
elk_version:    "7.17.21"

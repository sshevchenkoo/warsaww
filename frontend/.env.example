# The browser always calls the API via relative paths; Next proxies them to this
# upstream (see next.config.ts). Inside docker-compose use the service name.
BACKEND_INTERNAL_URL=http://localhost:8000
